# Compte-rendu — 2026-04-27

## Ce qu'on a fait aujourd'hui

**Phase 7 livrée et testée** — le système tourne en mode autonome continu. `run_continuous.py` consomme une queue de tâches (`tasks.json`), génère ses propres tâches quand la queue est vide, et s'appuie sur un mix Ollama local (Qwen 2.5 7B) + API Anthropic selon la complexité. 3 cycles de test validés, 10 appels Anthropic, heartbeat Ollama fonctionnel.

**Phase 8 livrée** — l'Executor détecte maintenant les `CODE_TASK:` dans les plans et délègue au `ClaudeCodeAgent`. Quand Claude Code CLI est installé, il reçoit la tâche en subprocess. Sinon, fallback API Anthropic Sonnet. Les artifacts (firmware, URDF, CAD) sont sauvegardés dans `data/artifacts/{type}/` et indexés dans la MEA.

**VISION.md finalisée** — roadmap complète : 4 morphologies de robot (tripode → quadrupède → appendice → bipède roues), 5 phases logicielles, boucle feedback sim→design, budget estimé ~330 CAD pour le hardware.

**Plan de communication établi** — Dispatch = canal principal. GitHub = source de vérité. Ce journal = compte-rendu quotidien pour les vidéos.

## Décisions importantes

**Morphologie multi-phases** : on ne choisit pas une morphologie unique. PROTO-1 (tripode) et PROTO-2 (quadrupède) démarrent en simulation en parallèle. PROTO-3 (appendice/queue) s'ajoute sur le meilleur candidat. PROTO-4 (bipède roues) est l'objectif long terme.

**Yoan = visionnaire/consultant, Askio1 = CEO autonome** : les décisions techniques sont prises sans validation. Les virages de direction passent par Dispatch.

**Tour GPU** : RTX 4090 recommandé (~1800 CAD), pas urgent. On continue avec API en attendant.

## Artifacts produits

- `askio1/agents/claude_code_agent.py` — délégation CODE_TASK vers Claude Code CLI
- `askio1/agents/executor.py` — routing CODE_TASK intégré
- `askio1/tools/llm_client.py` — multi-backend Anthropic + Ollama
- `askio1/agents/web_search.py` — DuckDuckGo + synthèse LLM + MEA
- `askio1/tools/heartbeat.py` — monitoring Ollama en thread daemon
- `run_continuous.py` — mode autonome infini
- `VISION.md` — roadmap complète robot + infrastructure
- `COMMUNICATION.md` — plan de communication
- `journal/generate_daily.py` — ce générateur de comptes-rendus
- `tasks.json` — 5 tâches robot (dont 3 CODE_TASK firmware/URDF/CAD)

## Métriques du jour

- Phases complétées : 7 + 8
- Fichiers poussés sur GitHub : 12
- Commits : 8
- MEA entrées : ~198
- Appels Anthropic (test) : 10
- Ollama : non disponible en sandbox (testé, fonctionnel en local)

## Prochaines étapes

1. **Installer Ollama** sur le laptop → `ollama pull qwen2.5:7b` → lancer `run_continuous.py` pour le premier vrai cycle autonome robot
2. **Phase 9** — stack simulation Gazebo : générateur URDF automatique depuis spécifications MEA, world + launch files ROS2, CPG controller
3. **Décision morphologie hardware** : après les 2 premières semaines de simulation, choisir entre PROTO-1 et PROTO-2 pour le premier hardware

---
*Généré par Askio1 v2 le 2026-04-27 — premier compte-rendu du journal*
